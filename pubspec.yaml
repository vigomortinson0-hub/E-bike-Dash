name: ebike_dash
description: "Minimalist E-Bike Dashboard for JK BMS and GPS"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^12.0.0
  flutter_blue_plus: ^1.31.0
  wakelock_plus: ^1.2.0

flutter:
  uses-material-design: true
  
