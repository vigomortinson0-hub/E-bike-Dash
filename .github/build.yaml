name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.0'

      - name: Rebuild Fresh Flutter App
        run: |
          # Сохраняем исходные файлы
          mkdir -p ../temp_code
          cp lib/main.dart ../temp_code/main.dart
          
          # Очищаем текущую директорию и создаём чистый проект Flutter
          rm -rf * .github
          flutter create --org com.ebike.dash .
          
          # Возвращаем наш файл main.dart
          cp ../temp_code/main.dart lib/main.dart
          
          # Добавляем необходимые зависимости
          flutter pub add geolocator flutter_blue_plus wakelock_plus

      - name: Build APK
        run: flutter build apk --release --no-tree-shake-icons

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: build/app/outputs/flutter-apk/app-release.apk
